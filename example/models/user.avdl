schema User;

import idl "address.avdl";

enum Status {
    ACTIVE,
    INACTIVE,
    PENDING,
    SUSPENDED
}

record User {
    string name;
    int id;
    string email;
    int age = 0;
    com.example.kentavro.Address address;
    Status status;
}
